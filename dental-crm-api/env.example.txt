# ===================================
# APPLICATION SETTINGS
# ===================================

# Application name - used in emails and UI
APP_NAME=CRM_DENTAL

# Application environment (local, development, staging, production)
APP_ENV=local

# Application key - generate with: php artisan key:generate
# IMPORTANT: Keep this secret and unique for each environment
APP_KEY=

# Enable debug mode (true/false) - MUST be false in production
APP_DEBUG=true

# Application URL - used for generating links in emails and notifications
APP_URL=http://localhost

# Application locale and fallback locale
APP_LOCALE=uk_UA
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=uk_UA

# Maintenance mode driver (file or database)
APP_MAINTENANCE_DRIVER=file

# Bcrypt hashing rounds - higher is more secure but slower
BCRYPT_ROUNDS=12

# ===================================
# LOGGING
# ===================================

# Log channel (single, daily, slack, stderr, syslog, errorlog, monolog, custom, stack)
LOG_CHANNEL=stack
LOG_STACK=single

# Deprecation warnings channel
LOG_DEPRECATIONS_CHANNEL=null

# Log level (debug, info, notice, warning, error, critical, alert, emergency)
LOG_LEVEL=debug

# ===================================
# DATABASE
# ===================================

# Database connection type (mysql, pgsql, sqlite, sqlsrv)
DB_CONNECTION=pgsql

# Database host (use 'pgsql' for Docker, 'localhost' or IP for local/remote)
DB_HOST=pgsql

# Database port
DB_PORT=5432

# Database name
DB_DATABASE=dental_crm_new

# Database credentials
DB_USERNAME=sail
DB_PASSWORD=admin

# ===================================
# SESSION & AUTHENTICATION
# ===================================

# Session driver (file, cookie, database, memcached, redis, dynamodb, array)
SESSION_DRIVER=redis

# Session lifetime in minutes
SESSION_LIFETIME=120

# Encrypt session data (true/false)
SESSION_ENCRYPT=false

# Session cookie path
SESSION_PATH=/

# Session cookie domain
SESSION_DOMAIN=localhost

# Laravel Sanctum - frontend domains that can make authenticated requests
SANCTUM_STATEFUL_DOMAINS=localhost:3000

# ===================================
# SEARCH (MEILISEARCH)
# ===================================

# Search driver (meilisearch, algolia, database, collection)
SCOUT_DRIVER=meilisearch

# Meilisearch host URL
MEILISEARCH_HOST=http://meilisearch:7700

# Meilisearch master key
MEILISEARCH_KEY=masterKey

# ===================================
# QUEUES & JOBS
# ===================================

# Queue connection (sync, database, beanstalkd, sqs, redis)
QUEUE_CONNECTION=redis

# ===================================
# CACHE
# ===================================

# Cache driver (file, array, database, memcached, redis, dynamodb)
CACHE_STORE=redis

# ===================================
# REDIS
# ===================================

# Redis client (phpredis or predis)
REDIS_CLIENT=phpredis

# Redis host (use 'redis' for Docker, 'localhost' for local)
REDIS_HOST=redis

# Redis password (leave null if no password)
REDIS_PASSWORD=null

# Redis port
REDIS_PORT=6379

# ===================================
# MEMCACHED (if used)
# ===================================

MEMCACHED_HOST=127.0.0.1

# ===================================
# MAIL
# ===================================

# Mail driver (smtp, sendmail, mailgun, ses, postmark, log, array)
MAIL_MAILER=log

# SMTP scheme (null, tls, ssl)
MAIL_SCHEME=null

# SMTP host
MAIL_HOST=127.0.0.1

# SMTP port (25, 587, 465)
MAIL_PORT=2525

# SMTP credentials
MAIL_USERNAME=null
MAIL_PASSWORD=null

# Email sender address and name
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ===================================
# BROADCASTING
# ===================================

# Broadcast connection (pusher, ably, redis, log, null)
BROADCAST_CONNECTION=log

# ===================================
# FILESYSTEM
# ===================================

# Default filesystem disk (local, public, s3, etc.)
FILESYSTEM_DISK=local

# ===================================
# AWS (if using S3 for file storage)
# ===================================

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ===================================
# FRONTEND
# ===================================

# Frontend application URL - used for generating links in emails
FRONTEND_URL=http://localhost:3000

# ===================================
# VITE (for asset bundling)
# ===================================

VITE_APP_NAME="${APP_NAME}"

