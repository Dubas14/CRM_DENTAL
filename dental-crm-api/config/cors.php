<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    */

    // Дозволяємо доступ до API
    'paths' => ['api/*', 'sanctum/csrf-cookie'],

    // Явно перелічуємо методи (щоб уникати "invalid CORS header values" у браузері)
    'allowed_methods' => ['GET', 'POST', 'PUT', 'PATCH', 'DELETE', 'OPTIONS'],

    // 👇 DEV origins (явний список найтиповіших адрес для Vite/Sail/WSL)
    // Якщо у тебе інший порт — додай сюди (або скажи мені, я додам).
    'allowed_origins' => [
        'http://localhost:3000',
        'http://localhost:5173',
        'http://127.0.0.1:3000',
        'http://127.0.0.1:5173',
        'http://wsl.localhost:3000',
        'http://wsl.localhost:5173',
    ],

    // Патерни вимикаємо, щоб уникати некоректних заголовків/поведінки в деяких оточеннях
    'allowed_origins_patterns' => [],

    // Явно перелічуємо заголовки (часто `*` для headers викликає попередження/блок у браузері)
    'allowed_headers' => [
        'Content-Type',
        'X-Requested-With',
        'Authorization',
        'Accept',
        'Origin',
        'X-XSRF-TOKEN',
        'X-CSRF-TOKEN',
    ],

    // Які заголовки можна читати фронтенду
    'exposed_headers' => [],

    // Кешуємо налаштування на 0 секунд (для розробки)
    'max_age' => 0,

    // 👇 ВАЖЛИВО: Оскільки ми прибираємо cookies, ставимо false.
    // Якщо колись захочеш cookies - поставиш true.
    'supports_credentials' => false,
];
